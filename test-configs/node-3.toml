# Node configuration for Participant 3

network_config = "network.toml"

[node]
participant_id = "participant-3"
static_key_file = "keys/participant-3.key"
listen_address = "0.0.0.0"

[canton]
admin_api_host = "localhost"
admin_api_port = 4902
ledger_api_host = "localhost"
ledger_api_port = 4901
synchronizer = "global"
