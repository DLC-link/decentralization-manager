# Node configuration for Participant 2

network_config = "network.toml"

[node]
participant_id = "participant-2"
static_key_file = "keys/participant-2.key"
listen_address = "0.0.0.0"

[canton]
admin_api_host = "localhost"
admin_api_port = 3902
ledger_api_host = "localhost"
ledger_api_port = 3901
synchronizer = "global"
